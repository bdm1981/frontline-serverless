{
  "name": "abcphone",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "twilio": "^3.56",
    "@twilio/runtime-handler": "1.2.0"
  },
  "devDependencies": {
    "twilio-run": "^3.3.0"
  },
  "engines": {
    "node": "12"
  }
}
